---
const {eyebrow,title,copy,center=false}=Astro.props;
---
<div class:list={["section-head",center&&"center"]}><span class="eyebrow">{eyebrow}</span><h2 set:html={title}/>{copy&&<p>{copy}</p>}</div>