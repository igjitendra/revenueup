const form=document.querySelector('[data-contact-form]');const status=document.querySelector('[data-contact-status]');form?.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent='Sending…';status.textContent='';try{const body=Object.fromEntries(new FormData(form).entries());const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const result=await response.json();if(!response.ok)throw new Error(result.error);status.textContent='✓ Thank you. Your enquiry has been delivered.';status.className='contact-status success';form.reset()}catch(error){status.textContent='Could not send automatically. Please use WhatsApp or try again.';status.className='contact-status error'}finally{btn.disabled=false;btn.innerHTML='Send enquiry <span>↗</span>'}});
